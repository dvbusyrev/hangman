# Facade textures

These lightweight SVG textures are bundled locally so the demo remains fast and fully offline.
They are simplified prototype facades inspired by CC0 concrete/panel references from Poly Haven.
You can replace any file without changing JavaScript; paths are configured in `public/data/config.json`.
